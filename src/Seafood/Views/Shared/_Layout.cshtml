<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css'>
    <link rel="stylesheet" type="text/css" href="~/css/styles.css" />

    <title>@ViewBag.Title</title>
</head>
<body>
    @using System.Security.Claims
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <!-- Brand and toggle get grouped for better mobile display -->
            <div class="navbar-header">
                <a class="navbar-brand" href="/Posts/Index">Home</a>
            </div>
            <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                <div class="nav navbar-nav">
                    <a class="navbar-brand" href="/Mailing/Index">Newsletter</a>
                    <a class="navbar-brand" href="/Account/Login">Admin Log in</a>
               </div>
                @if (User.Identity.IsAuthenticated)
            {
                    @using (Html.BeginForm("LogOff", "Account", new { @class = "navbar-form navbar-right" }))
                {
                        <input type="submit" class="btn btn-default" value="Log out" />
                    }
                }

            </div><!-- /.navbar-collapse -->
        </div><!-- /.container-fluid -->
    </nav>
    <div class="container">
        @RenderBody()
    </div>
</body>
</html>